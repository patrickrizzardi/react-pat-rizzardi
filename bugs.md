This is just bugs im writing down as i find them to fix later.

- on the /blog page the menu no longer works. we should probably jsut add blog TO the menu and make it cliable. Of course i know it is one page that scrolls to the wrigint section so ill take ux suggestions here. But we at least need the menu to work so users can navigate back to main site.

- not sure if it is bug or intentinal but the menu order doesnt follow page order. IE menu has systems then principles but the page has it reverse. Does that matter? 